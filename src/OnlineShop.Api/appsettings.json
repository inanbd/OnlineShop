{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": [
    "Two connection strings, never one.",
    "  ReadConnection  -> read replica; serves every query.",
    "  WriteConnection -> primary; serves every command and every transaction.",
    "Point ReadConnection at a replica listener and add ApplicationIntent=ReadOnly",
    "so SQL Server routes the session to a readable secondary.",
    "Real credentials belong in a secret store, not in this file."
  ],
  "ConnectionStrings": {
    "ReadConnection": "Server=READ-SERVER;Database=OnlineShop;Integrated Security=True;ApplicationIntent=ReadOnly;MultiSubnetFailover=True;Encrypt=True;TrustServerCertificate=False;Application Name=OnlineShop.Read;",
    "WriteConnection": "Server=WRITE-SERVER;Database=OnlineShop;Integrated Security=True;Encrypt=True;TrustServerCertificate=False;Application Name=OnlineShop.Write;"
  },

  "Persistence": {
    "EnforceCqrsConnectionRule": true
  },

  "Tenancy": {
    "//": "Trusting a request header for tenant identity is a development-only convenience. In production the tenant comes from the authenticated principal.",
    "AllowHeaderTenantResolution": false
  }
}
