{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "OnlineShop": "Debug"
    }
  },

  "//": [
    "Local development usually has one SQL Server instance, so both names point",
    "at it. Keeping them as two separate settings means the code paths stay",
    "identical to production - only the target differs.",
    "",
    "Because there is no replication lag here, a query that should have asked",
    "for ReadConsistency.Strong will look correct locally. Startup logs a",
    "warning when both strings match, as a reminder."
  ],
  "ConnectionStrings": {
    "ReadConnection": "Server=(localdb)\\MSSQLLocalDB;Database=OnlineShop;Integrated Security=True;TrustServerCertificate=True;Application Name=OnlineShop.Read;",
    "WriteConnection": "Server=(localdb)\\MSSQLLocalDB;Database=OnlineShop;Integrated Security=True;TrustServerCertificate=True;Application Name=OnlineShop.Write;"
  },

  "Tenancy": {
    "AllowHeaderTenantResolution": true
  }
}
